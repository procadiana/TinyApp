 <% include partials/_header %>
<body>
  <li><strong><a href="<%= urls[shortURL].longURL %>"><%= shortURL %> </strong> <a href="<%= urls[shortURL].longURL %>"> <%= urls[shortURL].longURL %> </a></li>
  <% if (owner) { %>
    <u><a href="/urls/<%= shortURL %>" display="inline"> Edit </a></u>
    <form method="POST" action="/urls/<%= shortURL %>/delete" display="inline">
    <a><button type="submit" class="btn btn-primary"> Delete </button></a>
    </form>

    <form class="form-inline" action="/urls/<%= shortURL %>" method="POST">
    <p><label for="longURL">Edit a URL:</label></p>
    <input type="text" name="longURL" placeholder="http://" style="width: 300px; margin: 1em">
    <button type="submit" class="btn btn-primary">Edit</button></p>
    </div>
    </form>
  <% } %>
</body>
</html>
